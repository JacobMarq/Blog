<% @articles = Article.all %>
<div id="articleCarouselControls" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
        <% top_articles = Article.top_three(@articles) %>
            <div class="carousel-item active">
                <% if top_articles[0].cover.attached? %>
                    <img src="<%= url_for(top_articles[0].cover) %>">
                <% else %>
                    <img>
                <% end %>
            </div>
            <div class="carousel-item">
                <%= top_articles[1] %>
            </div>
            <div class="carousel-item">
                <%= top_articles[2] %>
            </div>
        <% %>
    </div>

    <a class="carousel-control-prev" href="#articleCarouselControls" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#articleCarouselControls" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>