<div class="jumbotron bg-dark shadow">
    <h1 class="pt-4 pb-4">Share your voice and be heard</h1>
</div>
<br>

<br>
<div class="d-flex justify-content-center">
    <div class="border-bottom">
        <h1 class="text-center text-dark">Recent Articles</h1>
    </div>
</div>
<br>

<br>
<div class="container d-flex flex-column align-items-center">
        <% @articles = Article.all.order("created_at DESC") %>
        <% if @articles.length < 3 %>
            <p> No articles to render... </p>
        <% else %>
            <div class="shadow" style="max-height: 450px !important; max-width: 1100px !important">
                <%= render( :partial => "home/carousel", :locals => { :articles => @articles } )%>
            </div>
        <% end %>
    <br>

    <p class="mt-5">
        <%= button_to 'View All Articles', articles_path, :method => :get, :class => "btn btn-primary btn-lg" %>
    </p>
    <br>
</div>